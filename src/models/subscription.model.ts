class Subscription {
    constructor(title: string, status: boolean, total: number, currency: string, payCyle: string, type: string) {
        title = title;
        status = status;
        total = total;
        currency = currency;
        payCyle = payCyle;
        type = type;
    }
}

export default Subscription;